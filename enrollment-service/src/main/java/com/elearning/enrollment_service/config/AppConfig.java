package com.elearning.enrollment_service.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class AppConfig {
    // RestTemplate removed — course verification via HTTP not used
}
